# Preview run instructions

## Reproduce artifacts

1. From the project root, run `npm install` using the committed `package-lock.json`.
2. Run `npm run build` to generate `dist.html` from the TypeScript and CSS sources.

## View the standalone page

Open the generated `dist.html` directly in a browser or register it as the thread preview with its absolute path. No dev server or port is required.
