# Agent 协作规范

## CodeGraph 使用提示

本项目已配置 CodeGraph，可用于查看代码结构、节点关系、引用关系和受影响范围。需要了解模块关系时，优先使用 CodeGraph 查询；常用命令包括：

```bash
codegraph status
codegraph query <关键词>
codegraph affected <文件路径>
```

## CodeGraph 更新规则

**每次修改项目代码完毕后，必须更新 CodeGraph。** 在完成代码编辑后执行：

```bash
codegraph sync .
```

确认同步成功；若 CodeGraph 不可用，需在修改记录中说明原因，并在恢复可用后补同步。

## 进度记录规则

**每次对项目代码进行修改后，必须在 `progress/` 文件夹中新建一个 `.md` 文件，记录本次修改的内容。**

### 文件命名格式

```
progress/YYYY-MM-DD_简介.md
```

- **日期**：当天日期，格式 `YYYY-MM-DD`
- **简介**：简短描述修改内容（中文，不超过20字）
- **时间戳**：文件内记录具体完成时间

### 文件模板

```markdown
# 修改记录 - 简介

- **时间**: YYYY-MM-DD HH:MM:SS
- **修改类型**: 新功能 / 修复 / 重构 / 优化 / 文档

## 修改内容

（描述具体做了哪些修改）

## 影响范围

（哪些文件被修改，是否影响其他模块）

## 备注

（可选：后续待办、已知问题等）
```

### 示例

文件名：`progress/2026-08-26_新增存档系统.md`

```markdown
# 修改记录 - 新增存档系统

- **时间**: 2026-08-26 14:30:00
- **修改类型**: 新功能

## 修改内容

- 新增 `src/systems/saveLoad.ts` 实现 localStorage 存档
- 在 UI 中添加保存/加载按钮

## 影响范围

- 新增文件：`src/systems/saveLoad.ts`
- 修改文件：`src/ui/controller.ts`

## 备注

- 存档格式为 JSON，后续可考虑压缩
```

## 注意事项

1. **不要删除或覆盖**已有的 progress 文件
2. 每个文件只记录一次修改，不要追加
3. 简介要简洁明了，一眼看出改了什么
4. 如果一次修改涉及多个系统，在「影响范围」中列出所有相关文件
