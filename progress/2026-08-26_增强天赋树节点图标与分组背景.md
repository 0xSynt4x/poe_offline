# 天赋树节点图标与分组背景增强

日期：2026-08-26

## 完成内容

- 从 GGG 天赋树数据导出分组中心、轨道半径、成员节点和背景标记。
- 在连线下方绘制 PoB 风格的分组底纹、外轮廓和轨道环。
- 按节点类型区分普通天赋、重要天赋、关键天赋、升华天赋和珠宝孔的外框与中心 glyph。
- 保留已分配、可分配、搜索命中和 hover 状态，不改变天赋分配逻辑。
- 适配缩放时隐藏普通节点标签，放大、搜索命中或已分配时显示标签，降低全树视图的文字密度。

## 验证

- 重新运行 `node scripts/convertTree.js` 生成天赋树数据。
- `npx tsc --noEmit` 通过。
- `npm run build` 通过，生成 `dist.html`。
- 预览确认分组背景和节点图标可见，默认全树视图无明显标签堆叠。
