# 修改记录 - 运行时行为验证修复

- **时间**: 2026-08-26
- **修改类型**: Bug修复 + 测试

## 修改内容

- 修复 `build.ts` 全局字符串替换 HTML 占位符的问题。
- 改为按模板占位符位置拼接 CSS 和 JS，避免 bundle 内相同文本被误替换，导致 `dist.html` 内联脚本解析失败。
- 运行时验证期间补充并确认探索奖励统一由控制器发放，避免 ZoneSystem 与 UI 双重发放。
- 探索奖励补充 currency/item ID，保证统一发放逻辑能够正确识别奖励。

## 验证结果

- `npm run build` 通过。
- `npx tsc --noEmit` 通过。
- 使用 Node VM 解析生成的内联脚本通过。
- Headless 生命周期断言通过：区域探索、经验发放、任务完成、任务奖励一次性领取、宝石镶嵌/取下、战斗消耗魔力。
- 注册 `dist.html` 预览并实际操作通过：新游戏初始化、背包导航、地图仪打开/关闭、主区域唯一可见。
- `game.runRegressionChecks()` 全部返回 true。
- `preview_logs` 无 console/page 错误。

## 影响范围

- 修改文件：`build.ts`
- 修改文件：`src/systems/zone.ts`
- 修改文件：`src/ui/controller.ts`
- 新增文件：本进度记录
